<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # If already in /public, skip
    RewriteCond %{REQUEST_URI} !^/public/

    # Redirect EVERYTHING to public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>